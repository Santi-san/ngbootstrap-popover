import { PopcloseDirective } from './popclose.directive';

describe('PopcloseDirective', () => {
  it('should create an instance', () => {
    const directive = new PopcloseDirective();
    expect(directive).toBeTruthy();
  });
});
